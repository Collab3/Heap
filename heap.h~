#ifndef HEAP_H
#define HEAP_H

#include<iostream>
#include<string>


class heap{
 private:
  int k; //match class definitions
  vector<int> buffer;
 public:
  heap();
  int size();
  void insert(int value);
  void remove_max();
  int max();
  void print();
};

#endif //heap.h
